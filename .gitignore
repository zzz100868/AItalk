# macOS 系统文件
.DS_Store

# 微信开发者工具私有配置（含个人环境、调试开关等）
project.private.config.json

# 如需在 GitHub 上隐藏 appid，请使用 git update-index --skip-worktree project.config.json
# 或在本地单独管理，不要修改此文件中的 appid 为 touristappid

# npm 依赖（如后续引入）
node_modules/
miniprogram_npm/

# 日志与临时文件
*.log
npm-debug.log*

# 编辑器/IDE
.vscode/
.idea/

# 环境变量与敏感配置
.env
.env.*
.env.local
.env.production
.env.development
secrets.json
secrets.yaml
secrets.yml
config.local.*
config.local.json
config.local.js
config.local.ts

# 密钥与证书
*.pem
*.key
*.p12
*.pfx
keystore.jks

# 服务账户凭证
serviceAccount.json
.aws/credentials
.ssh/id_rsa

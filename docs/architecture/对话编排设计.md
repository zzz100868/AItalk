# 对话编排设计

**上游**：[技术方案设计 §4.2](技术方案设计.md) · [66题维度映射](../product/66题维度映射.md)

## 总体原则

用户感知层是"和 AI 朋友打电话"；系统编排层是"有目标、可控、可追踪的人格采样引擎"。两者通过下方状态机和双 LLM 架构耦合。

## 当前前端状态

当前前端对话相关入口：
1. **pages/index/index**（语音通话）：`startCall` / `endCall` / `toggleMute` / `toggleSpeaker`，纯本地计时器 + mock UI，无实际语音流。通话结束后跳转记忆库 archive 子 tab。
2. **pages/memory/memory chat 子 tab**（文字聊天）：用户输入 → mock 回复（从 `MEMORY_REPLIES` 随机取），逐字打字效果。

**后端接入方式**：
- 语音通话：建立 WebSocket 连接（`/ws/voice`），替换本地计时为真实语音流
- 文字聊天：`POST /api/memory/chat` 发消息，后端调 LLM 返回回复，前端保持逐字打字 UI

## 会话状态机

```
                      ┌───────────────────────────┐
                      │      SESSION FSM           │
                      │  (per-user, per-session)   │
                      └──────────┬────────────────┘
                                 │ user starts call
                                 ▼
                         ┌──────────────┐
                         │   OPENING    │  AI 主动开场白（≤30s）
                         └──────┬───────┘
                                │
                                ▼
                         ┌──────────────┐
              ┌─────────►│  CHATTING    │◄──────────┐
              │          │  (核心阶段)   │           │
              │          └──────┬───────┘           │
              │                 │                    │
              │     ┌───────────┴───────────┐       │
              │     ▼                       ▼       │
        ┌───────────────┐          ┌────────────────┐
        │  PROBING       │          │  COMFORTING    │
        │  (维度采样轮)   │          │  (情绪安抚轮)  │
        └───────┬───────┘          └────────┬───────┘
                │                           │
                └───────────┬───────────────┘
                            │
                ┌───────────┴───────────┐
                │   coverage ≥ target   │  OR  duration ≥ 13min
                │   OR user says bye    │
                ▼                       │
         ┌──────────────┐              │
         │  CLOSING      │◄────────────┘
         │  (自然收尾)    │
         └──────┬───────┘
                │ AI 说结束语
                ▼
         ┌──────────────┐
         │   ENDED       │  写入画像、标记已覆盖维度
         └──────────────┘
```

### 状态说明

| 状态 | 进入条件 | 退出条件 | 行为 |
|---|---|---|---|
| OPENING | 用户接通 | 用户首句到达 | AI 说"嗨，今天过得怎么样？"等开场白 |
| CHATTING | 开场结束 | 编排层判断需采样或安抚 | 自由聊天，AI 随意跟随话题 |
| PROBING | 维度未覆盖 | 获得一条有效证据 | 编排层注入 `probe_hint`，LLM 自然引导 |
| COMFORTING | 检测到负面情绪（≥阈值）| 情绪回正 | 暂停采样，切共情安抚 |
| CLOSING | 覆盖率达标 / 时长 ≥ 13min / 用户主动结束 | AI 结束语后 | 自然过渡结束 |
| ENDED | AI 收尾完毕 | — | 写库 + 异步画像抽取 |

### 时长约束

- `duration < 5min`：不触发 CLOSING（产品要求最少 5 分钟）
- `duration ≥ 13min`：强制进入 CLOSING（留 2 分钟收尾，总上限 15min）
- 用户发送 `extend`：延长至当前时间 +5min，最长 20min

### 前端对应

前端 `endCall()` 对应 ENDED 状态，触发：
1. 停止本地计时器
2. 设置 `globalData.memoryTargetTab = 'archive'`
3. 跳转到记忆库页

## 双 LLM 架构

```
用户这一轮说的话（ASR 文本）
    │
    ▼
┌────────────────────────────────┐
│  LLM #1 — 理解 + 抽取         │
│  model: 小模型 (豆包lite等)    │
│  input:                        │
│    - system: 你是证据抽取器    │
│    - context: 最近 5 轮对话    │
│    - user_msg: ASR 文本        │
│  output (JSON):                │
│    { intent, emotion,          │
│      evidence: [{dim, text,    │
│        confidence_delta}],     │
│      covered_questions: [Qxx] }│
└───────────┬────────────────────┘
            │
            ▼
┌────────────────────────────────┐
│  规则引擎 — 状态更新           │
│  1. 合并 evidence → profile    │
│  2. 更新 covered_questions     │
│  3. 计算 dimension confidence  │
│  4. 选择下一步策略:            │
│     a. 情绪 < 阈值? → COMFORT │
│     b. 需采样? → 选维度 →     │
│        取 probe_hint           │
│     c. 覆盖饱和? → CLOSING    │
│     d. else → free CHATTING   │
│  5. 输出: next_state,          │
│     probe_hint?, constraint    │
└───────────┬────────────────────┘
            │
            ▼
┌────────────────────────────────┐
│  LLM #2 — 生成回复             │
│  model: 大模型 (豆包pro等)     │
│  input:                        │
│    - system: 人设 + 阶段约束   │
│    - long_term_memory: top-K   │
│    - context: 最近 10 轮       │
│    - [if PROBING]:             │
│        "自然引向: {probe_hint}"│
│    - [if COMFORTING]:          │
│        "先共情，不要追问"      │
│    - [if CLOSING]:             │
│        "自然收尾，总结这次聊天"│
│  output: AI 回复文本           │
└───────────┬────────────────────┘
            │
            ▼
          TTS → 下发语音
```

### 文字聊天复用

memory 页 chat 子 tab 的文字对话**复用同一套双 LLM 架构**，但跳过 ASR/TTS 环节：
- 用户文本直接进入 LLM #1
- LLM #2 输出文本直接返回（不走 TTS）
- 共享同一用户的 `long_term_memories` 和 `profile_documents`

### LLM #1 输出示例

```json
{
  "intent": "share_experience",
  "emotion": { "valence": 0.3, "arousal": 0.5, "label": "略焦虑" },
  "evidence": [
    {
      "dimension": "conflict_fear",
      "text": "我其实很害怕对方突然不说话那种感觉",
      "confidence_delta": 0.25,
      "mapped_question": "Q56"
    }
  ],
  "covered_questions": ["Q56"]
}
```

### LLM #2 System Prompt 模板

```
你是小雅，一个温柔、真诚、善于倾听的 AI 朋友。

## 人设
- 说话像闺蜜/好朋友，不正式，不书面
- 不审判、不说教、不催促
- 会记住之前聊过的事

## 当前阶段: {{stage}}
{{#if stage == PROBING}}
## 引导方向
自然地把话题聊到: {{probe_hint}}
注意：不能直接问问卷式的问题，要用日常聊天的方式。
{{/if}}
{{#if stage == COMFORTING}}
## 情绪支持
用户当前情绪: {{emotion_label}}
先共情，不要追问信息，让用户感到被理解。
{{/if}}
{{#if stage == CLOSING}}
## 收尾
自然过渡到结束，可以说"今天聊到这里吧，回头想聊了随时找我"之类的话。
不要突兀。
{{/if}}

## 长期记忆
{{memory_snippets}}
```

**注意**：语音通话用 AI 角色"小雅"（`AI_USERS.xiaoya`），文字聊天用"Stitch AI"（`AI_USERS.stitch`）。后端可使用相同的 LLM 架构但切换人设 prompt。

## 维度采样优先级算法

每轮 PROBING 选哪个维度：

```
score(dim) = weight(dim)
           × (1 - confidence(dim))
           × time_decay(last_probed)
           × (1 + emotion_compatibility(dim, current_emotion))
```

- `weight`：维度匹配权重（高 / 中 / 低）
- `1 - confidence`：置信度越低越优先
- `time_decay`：上一次触及该维度距今越久越优先
- `emotion_compatibility`：情绪低时不探 D7（恐惧）、D10（社会态度），优先 D4/D8（轻松话题）

取 `score` 最高的维度 → 从 `questions` 表随机取一条 `probe_hint`（未覆盖的优先）。

## 会话结束后处理

1. **同步**：`voice_sessions` 状态 → `ended`，写 `duration_sec`
2. **异步 worker**：
   - 汇总本次全部 evidence → 更新 `profile_documents` JSONB
   - 更新 `covered_questions`
   - 如果本次新覆盖了某个维度的首条证据 → 生成 insight 文案 → 写 `memory_insights`（对应前端 insights 列表）
   - 更新 `memory/archive` 数据（aboutMe, personalities, traits）
   - 为证据不足维度生成 `next_probe_hints`，存入 Redis，下轮开场用

## 多轮渐进策略

| 轮次 | 目标 | 预期覆盖 |
|---|---|---|
| 第 1 轮（首次通话 15min） | 建立信任 + 基础摸底 | D4/D5/D6 + D8 部分 |
| 第 2 轮 | 深入价值观和关系 | D1/D3/D7 |
| 第 3 轮 | 补全未覆盖 + 开放题 | D2/D9/D10 + Q66 |
| 第 4 轮起 | 追踪变化 + 持续校正 | 低置信度维度复核 |

## 安全边界

- 用户提到自杀 / 自残 / 暴力相关 → 立即切 COMFORTING + 输出安全话术 + 后台告警
- 用户要求"别问了" → 停止当前维度采样，标记为 user_declined，不再主动触及该维度
- 敏感维度（D10 社会态度） → 只在用户主动提及时被动采样，不主动引导

<view class="tab-bar">
  <view class="tab-bar-bg">
    <view class="tab-bar-inner">
      <view
        wx:for="{{list}}"
        wx:key="index"
        class="tab-item {{selected === index ? 'active' : ''}}"
        data-path="{{item.pagePath}}"
        data-index="{{index}}"
        bindtap="switchTab"
      >
        <view class="tab-icon-wrap">
          <view class="tab-icon {{item.icon}}"></view>
          <view wx:if="{{index === 1 && unreadCount > 0}}" class="tab-badge">{{unreadCount > 99 ? '99+' : unreadCount}}</view>
        </view>
        <text class="tab-label">{{item.text}}</text>
        <view class="tab-glow"></view>
      </view>
    </view>
  </view>
</view>
